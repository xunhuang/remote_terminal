name: mac
on:
  workflow_dispatch:
defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - run: source script-mac.sh
      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        with:
          detached: true
          connect-timeout-seconds: 21600
      - name: print tmate session
        run: |
          /tmp/tmate/tmate -S /tmp/tmate.sock display -p '#{tmate_ssh}'
      - name: print tmate session
        run: |
          url=`/tmp/tmate/tmate -S /tmp/tmate.sock display -p '#{tmate_ssh}' |jq "@uri" -jRr `; echo $url
